import 'package:flutter/material.dart';

class EditNoteViewBody extends StatelessWidget {
  const EditNoteViewBody({Key?key}): supper(key:key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}